---
title: 院試勉強20250727
created: 2025-07-27
prev: entrance-exam/20250725
next: entrance-exam/20250728
tags:
- graph-theory
- information-theory
- computer-architecture
- computer-system
- guide
modified: 2025-07-29
---

説明問題のあてとして、講義で使っていた教科書とか資料をあたったりするのが正しい気がしてきた。
あと実験のときに実装したプロセッサに関する事項とか。

# グラフ理論

## 隣接行列

頂点どうしの隣接関係を行列にしたもの。自分自身とは隣接しないので、対角成分は0。

## 接続行列

頂点と辺の接続関係を行列にしたもの。頂点と辺に適当に番号を振って、行を頂点、列を辺として頂点 $i$ と辺 $j$ が接続していたら $(i,j)=1$ とする。

## Diracの定理

ハミルトン閉路を持つことの同値条件。頂点の次数が... (TODO)

## Oreの定理

Diracの定理の強い版。任意の2頂点に対して、次数の和が頂点の数以上であることと、ハミルトン閉路を持つことが同値というもの。
証明が重要。

## 同型

二つのグラフ $G=(V,E),G'=(V',E')$ を単純グラフとすると、$G$ の任意の2頂点 $v,w\in V$ に対して、$(v,w)\in E$ となるのが、$(f(v),f(w))\in E'$ となるとき、かつそのときに限るような $V$ から $V'$ への全単射写像 $f$ が存在するとき、
$G, G'$ は同型という。同型性判定問題はNPらしい。

# エントロピー

## 結合エントロピー

$$
H(X,Y) = -\sum_{i=0}^{M_X}\sum_{j=0}^{M_Y} p(x_i,y_j)\log_2{p(x_i,y_j)}
$$

## 条件付きエントロピー

$$
H(X|Y) = -\sum_{i=0}^{M_Y}p(y_i)\sum_{j=0}^{M_X}p(x_j|y_i)\log_2{p(x_j|y_i)}
$$

結合エントロピーとの関係は、

$$
H(X,Y) = H(Y) + H(X|Y)
$$

基本的に $H(X)$ より小さくなるはず。

## 相互情報量

Yを知ることで、Xの曖昧さがどのぐらい減ったかをそのエントロピーの差で計算できる。

$$
I(X;Y) = H(X) - H(X|Y)
$$

この値は必ず0以上になるので、負になった場合はどこかで計算を間違えている。

# 計算機システム

## 符号拡張

最上位の符号ビットを拡張するビット分左に並べる。論理右シフトの逆みたいな感じ。

## アドレッシング

命令の操作対象の指定と、主記憶のデータの参照の方法について。

### 即値

命令中で与えられた即値をオペランドとする。

### レジスタ

命令中で与えられた番号のレジスタの値をオペランドとする。

### ベース相対

レジスタの値と即値の和のアドレスを参照する（データアドレッシング）。

### PC相対

現在のPCからどれだけ先（前）の番地にアクセスするかを指定する（データアドレッシング）。

# 頭の整理

演習がたりていない分野を確認。分野レベルで不足しているのは計算と論理（まだ過去問を解いてない）。
情報理論、計算機アーキテクチャ、計算機システムの説明問題。プログラミング言語処理系とOSの演習。グラフ理論の用語理解、アルゴリズムとデータ構造の演習。

演習不足もあるが、結構理解を後回しにしている項目が目立ってきた。
